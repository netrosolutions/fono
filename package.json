{
  "name": "fnetro-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build":     "npm run build -w packages/fnetro && npm run build -w packages/create-fnetro",
    "typecheck": "npm run typecheck -w packages/fnetro && npm run typecheck -w packages/create-fnetro",
    "clean":     "npm run clean -w packages/fnetro && npm run clean -w packages/create-fnetro"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
